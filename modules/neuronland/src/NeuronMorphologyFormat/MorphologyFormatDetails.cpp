//
//
//


#include "MorphologyFormatDetails.h"


MorphologyFormatDetails::MorphologyFormatDetails()
{

}

MorphologyFormatDetails::MorphologyFormatDetails(const String& name)
{
	m_name = name;
}


MorphologyFormatDetails::MorphologyFormatDetails(class Input& rIn)
{
//	Strings xmlFile(&rIn);
//	Strings morphologyFormat = xmlFile.extract("morphologyFormat");

//	m_name								= morphologyFormat.extractLine("name");

	/*
	m_description						= morphologyFormat.extractString("description");

	m_software							= morphologyFormat.extract("origin").extractString("software");
	m_dataSource						= morphologyFormat.extract("origin").extractString("dataSource");

	//m_extensions						= morphologyFormat.extract("extensions");

	m_dataFileEncoding					= morphologyFormat.extract("dataFileEncoding");
	m_somaSupport						= morphologyFormat.extract("somaSupport");
	m_axonSupport						= morphologyFormat.extract("axonSupport");
	m_dendriteSupport					= morphologyFormat.extract("dendriteSupport");
	m_geometryRepresentation			= morphologyFormat.extract("geometryRepresentation");
	m_cellMarkers						= morphologyFormat.extract("cellMarkers");
	m_somaMarkers						= morphologyFormat.extract("somaMarkers");
	m_peerlessRoot						= morphologyFormat.extract("peerlessRoot");
	m_branchingLimit					= morphologyFormat.extract("branchingLimit");
	m_branchMarkers						= morphologyFormat.extract("branchMarkers");
	m_branchExplicitConnectivity		= morphologyFormat.extract("branchExplicitConnectivity");
	m_branchRepeatBranchPointSample		= morphologyFormat.extract("branchRepeatBranchPointSample");
	m_branchOrderLimit					= morphologyFormat.extract("branchOrderLimit");
	m_branchMarkers						= morphologyFormat.extract("branchMarkers");
	m_sampleUnitsPosition				= morphologyFormat.extract("sampleUnitsPosition");
	m_sampleUnitsDiameter				= morphologyFormat.extract("sampleUnitsDiameter");
	m_visualMarkerColours				= morphologyFormat.extract("visualMarkerColours");
	m_visualBranchColours				= morphologyFormat.extract("visualBranchColours");
	*/
}
